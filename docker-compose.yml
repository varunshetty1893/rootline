services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rootline
    ports:
      - "5432:5432"
    volumes:
      - rootline_pg_data:/var/lib/postgresql/data

volumes:
  rootline_pg_data:
